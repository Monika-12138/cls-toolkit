# DUT (Device Under Test) pre-flight info -- fill once, injected into every tool command.
# Copy to dut.yaml and fill in real values (dut.yaml is gitignored).

# -- Device identity (used for the report's DUT Identification) --
toe_name: ASKEY RTM7230T
model: RTM7230T-D171
firmware_version: "1.0"
vendor: ASKEY

# -- Network --
ip: 192.168.1.1                 # DUT IPv4
lan_subnet: 192.168.1.0/24      # LAN subnet (used by netdiscover)
portal_url: https://192.168.1.1 # config portal URL (used by web tests)
wifi_ssid: ""
wifi_password: ""

# -- Credentials --
default_user: admin
default_pass: admin

# -- Files (absolute paths on this host) --
firmware_file: ""               # firmware .bin (for binwalk)
firmware_extract_dir: ""        # directory binwalk extracted to (for firmwalker)
apk_file: ""                    # Android APK (for MobSF)

# -- Wordlists (for hydra etc.) --
users_file: /usr/share/wordlists/metasploit/unix_users.txt
pass_file: /usr/share/wordlists/rockyou.txt

# -- Report metadata --
tester: zicheng
lab: ""
etr_version: "1.0"
date: "2026-06-19"
