# Test pipeline -- ordered list of tool ids to run.
# Equivalent to "drag tools into the workspace and order them" on the old website idea.
# Tool ids: python3 cls.py list

pipeline:
  - nmap        # PS-1  port/service scan
  - testssl     # CO-3  TLS config check
  - dirsearch   # CP-1  hidden path discovery
  - sqlmap      # CP-3  SQL injection
